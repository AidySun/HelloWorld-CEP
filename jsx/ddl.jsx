$._CEP_CC = {	allOptionsHTML : function() {		var menuActions = app.menuActions;		var allOptions = "";		for (var i = 0; i < menuActions.length; i++) {			var showStr = menuActions[i].name;			if (menuActions[i].area != null && menuActions[i].area.length > 0) {				showStr = menuActions[i].area + "&gt" + menuActions[i].name;			}			allOptions += "<option value=\"" + menuActions[i].id + "\">" + showStr + "</option>\n";		}		return allOptions;	},	selectWithOptions : function() {		var options = $._CEP_CC.allOptionsHTML();		//if(options != undefined && options.length > 0) {			var select = '<select id="myselect"  class="myselectstyle"   autofocus=true>' + options + '</select>';			return select;		//}		//return "";	},}